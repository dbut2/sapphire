name: Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  release:
    name: Build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
      - run: |
          make build
