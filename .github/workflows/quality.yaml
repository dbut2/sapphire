name: Quality

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get update
      - run: sudo apt-get install --fix-missing libx11-dev libxcursor-dev libxrandr-dev libxinerama-dev libxinput-dev libgl1-mesa-dev
      - uses: actions/setup-go@v3
        with:
          go-version: '1.20'

      - uses: golangci/golangci-lint-action@v3

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get update
      - run: sudo apt-get install --fix-missing libx11-dev libxcursor-dev libxrandr-dev libxinerama-dev libxinput-dev libgl1-mesa-dev
      - uses: actions/setup-go@v3
        with:
          go-version: '1.20'

      - run: go test ./...
